---
'shutteros-playground': patch
---

Start a release from develop with one command. GitHub handles Changesets versioning, verified release and return merges, tags and publication without an open terminal. Feature merges remain independent of release requests.
