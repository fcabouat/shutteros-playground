---
'shutteros-playground': patch
---

Use a repository-scoped GitHub App for release PR creation so ordinary PR checks start without extra workflow approval. Remove duplicate verification dispatches, validate the tested merge and release identity, and report failed delivery stages with recovery guidance.
